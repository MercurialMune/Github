import { TextshadowDirective } from './textshadow.directive';

describe('TextshadowDirective', () => {
  it('should create an instance', () => {
    const directive = new TextshadowDirective();
    expect(directive).toBeTruthy();
  });
});
